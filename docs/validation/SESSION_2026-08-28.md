# Session report — 2026-08-28 (initial build session)

## Implemented

- Repository baseline: `git init` (main), governing specs archived under `docs/specs/`,
  governance + community + citation documents, zero-install launchers (macOS `.command` /
  Windows `.bat`), copyright firewall in `.gitignore`.
- Phase 0: `SOURCE_REGISTER.md` with per-source verification status; Approved Document O
  2021 downloaded from gov.uk and machine-verified
  (`docs/standards/ADO_VERIFICATION.md`, SHA-256 `36abadd3…`); rule-pack JSON Schema
  (`packages/overheatlens-core/overheatlens/schemas/rule_pack.schema.json`) + validated loader.
- Phase 1: `overheatlens-core` package skeleton — provenance (hashing, manifests), schemas,
  CLI (`version`, `rule-packs`, `check-epw`, `passport`), hub self-check entry, GitHub Actions CI.
- Phase 2 (partial): EPW parser, calibrated QC checker, headline metrics.
- Phase 3 (partial): standards engine with TM59:2017 criteria A/B/C, evaluation gate,
  Part O pack (inheriting criteria, carrying machine-verified ADO limits).
- Four rule packs incl. the deliberately empty, blocked TM59:2026 scaffold.

## Scientific decisions

- **ADO 2021 dynamic route verified**: cites TM59 **2017**; §2.6 window-control limits
  (22/26 °C day hysteresis; 23 °C-at-23:00 night condition; accessibility conditions;
  entrance door shut) encoded as first-class model limits; §2.8/2.9 exclusions (internal
  blinds, curtains, foliage not creditable). ADO does NOT name a weather file — the
  requirement is inherited from TM59:2017 (negative finding recorded).
- **TM59:2026 blocked**: no criteria value invented; pack structurally incapable of evaluation
  (ADR-0005). Web-search quota reset 2026-08-30 will allow capturing CIBSE public statements.
- **TM59:2017 values are SECONDARY-PENDING**: thresholds (3 %, 1 %, 32 h) transcribed from
  widely published secondary literature, flagged in pack + register; compliance mode refused
  in code until the original document is checked.
- **EnergyPlus working pin 25.1.0-68a4a7c774** (installed, version-verified); 26.1.0 upgrade
  deferred to an explicit pre-release pin change (ADR-0004).
- **Sleep-window geometry**: 22:00–07:00 implemented as hour-ending labels 23,24,1..7; the
  boundary test suite caught an initial off-by-one (label 22 vs label 7) which was fixed
  before any result was produced (VAL-STD-07).

## Tests run

```
PYTHONPATH=packages/overheatlens-core python -m pytest packages/overheatlens-core/tests -q
→ 65 passed in 1.75s (0 failed, 0 skipped)
PYTHONPATH=… python -m pytest … --cov=overheatlens --cov-report=term
→ 85% total; standards/engine.py 96%, epw/validation.py 93%, epw/parser.py 89%
bash "Start OverheatLens.command"   → self-check dashboard, fixture PASS
python -m overheatlens check-epw fixtures/epw/synthetic/temp_spike.epw → FAIL (DISCONTINUITY)
```

Property-based tests (hypothesis): exceedance monotonicity under uniform heating,
shift invariance (float-tie cases excluded, documented), order invariance.

## Visual QA

Terminal only this session (web UI is Phase 8): Start launcher dashboard inspected —
pack status table, honest "REFUSED (not source-verified)" labels, fixture self-check PASS.
No UI claims are made anywhere in the product.

## Validation evidence

Rows added to `VALIDATION_MATRIX.md`: V-EPW-01..13, VAL-STD-01..08, VAL-PRV-01..02 —
all PASS. Failure-injection coverage: 5 of 8 synthetic fixtures carry planted faults;
every planted fault is detected with the expected code and severity.

## Open blockers

1. CIBSE originals (TM59:2017, TM59:2026, TM52, weather guidance) not acquired —
   blocks compliance-mode evaluation of those packs (by design).
2. Web-search quota resets 2026-08-30 — capture CIBSE TM59:2026 announcements (S-05).
3. No UI/simulation yet — Phases 4–8 next.

## Next implementation step

Phase 5 (IDF/epJSON readiness + IDF Passport) in parallel with Phase 4 (comfort wrappers
around pythermalcomfort 4.4.2), then the EnergyPlus worker (Phase 6) so end-to-end
archetype regressions can freeze.
